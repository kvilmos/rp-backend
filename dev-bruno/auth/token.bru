meta {
  name: token
  type: http
  seq: 4
}

post {
  url: http://localhost/api/auth/token
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "token"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
